program Lab4;

{$APPTYPE CONSOLE}
{$R *.res}

uses
  System.SysUtils;

type
  TIntegral = function(X: Real): Real;

function RectangleMethod(Integ: TIntegral; A, B, E: Real;
  var Count: integer): Real;
var
  N: integer;
  H, Sum: Real;
  I: integer;
begin
  N := 5;
  H := (B - A) / N;
  Sum := 0;
  for I := 0 to (N - 1) do
  begin

  end;
end;

begin
  try
    { TODO -oUser -cConsole Main : Insert code here }
  except
    on E: Exception do
      Writeln(E.ClassName, ': ', E.Message);
  end;

end.
